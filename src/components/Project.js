import React from 'react';
// import PropTypes from 'prop-types';

const Project = ({}) => {
  return (
    <div>One Project</div>
  );
}

// Project.propTypes = {
//   : PropTypes.
// };

export default Project;
